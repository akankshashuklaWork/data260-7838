# AI Use Disclosure



1. I used an AI assistant to help organize the repository, develop the Part 1 HTML/JavaScript form, write Docker configuration, and create automated source checks. I reviewed the UI and performed the local and AWS deployment steps.
2. An earlier repository layout placed the application inside a homework-specific folder, which was unsuitable because future homework must extend one shared codebase.
3. I detected the issue by comparing the repository layout with the instruction that application code must remain in shared root-level `code/` and `src/` folders.
4. I moved the single web application to `code/web_application/`, placed `src/model_client.py` at its exact required path, and reserved `reports/hw01/` only for evidence and report artifacts.
5. I used AI for paraphrasing at some of the places.
